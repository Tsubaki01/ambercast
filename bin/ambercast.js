#!/usr/bin/env node
const { version } = require('../package.json');

console.log(`ambercast v${version}`);
console.log('');
console.log('Prompt-native E2E testing: AI compiles your natural-language test');
console.log('prompts into a deterministic execution plan, replayed with zero AI calls.');
console.log('');
console.log('This package is under active development. The CLI is not functional yet.');
